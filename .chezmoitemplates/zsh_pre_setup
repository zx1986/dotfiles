WORDCHARS='*?[]~&;!#$%^(){}<>'

LANG='en_US.UTF-8'
LC_ALL='en_US.UTF-8'
LC_CTYPE='UTF-8'

EDITOR='nvim'
VISUAL='nvim'

{{ if eq .chezmoi.os "darwin" -}}
# macOS-specific
if [ -x /opt/homebrew/bin/brew ]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
elif [ -x /usr/local/bin/brew ]; then
  eval "$(/usr/local/bin/brew shellenv)"
fi

OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES
FZF_BASE="/opt/homebrew/bin/fzf"
ZSH_FUNC_DIR="/usr/local/share/zsh/site-functions/"
{{ else -}}
# Linux-specific
FZF_BASE="/usr/bin/fzf"
ZSH_FUNC_DIR="/usr/share/zsh/vendor-functions/"
{{ end -}}

# Kubernetes
KUBECONFIG_PATH=~/.kube
KUBECONFIG=~/.kube/config
PATH="${KREW_ROOT:-$HOME/.krew}/bin:/usr/local/sbin:$PATH"

# ASDF
PATH="${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH"

# fzf
FZF_DEFAULT_COMMAND='fd --type file --follow --hidden --exclude .git --color=always'
FZF_DEFAULT_OPTS="--ansi"
FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"

# Ensure spaceship theme is in fpath
if [ -d "$HOME/.zprezto/contrib/contrib-prompt/functions" ]; then
  fpath=("$HOME/.zprezto/contrib/contrib-prompt/functions" $fpath)
fi

# Zsh runtime configuration
#ENABLE_CORRECTION="true"
#CASE_SENSITIVE="true"
HYPHEN_INSENSITIVE="true"
COMPLETION_WAITING_DOTS="true"
DISABLE_UNTRACKED_FILES_DIRTY="true"
HIST_STAMPS="yyyy-mm-dd"
HISTFILE=$HOME/.zsh_history
HISTSIZE=100000
SAVEHIST=$HISTSIZE

# Spaceship theme configuration
SPACESHIP_KUBECTL_SHOW="true"
SPACESHIP_PROMPT_ADD_NEWLINE="false"
SPACESHIP_PROMPT_ORDER=(
  time           # Time stamps section
  user           # Username section
  dir            # Current directory section
  host           # Hostname section
  git            # Git section (git_branch + git_status)
  package        # Package version
  node           # Node.js section
  ruby           # Ruby section
  python         # Python section
  golang         # Go section
  php            # PHP section
  docker         # Docker section
  docker_compose # Docker section
  kubectl        # Kubectl context section
  ansible        # Ansible section
  terraform      # Terraform workspace section
  pulumi         # Pulumi stack section
  exec_time      # Execution time
  async          # Async jobs indicator
  line_sep       # Line break
  battery        # Battery level and status
  jobs           # Background jobs indicator
  exit_code      # Exit code section
  sudo           # Sudo indicator
  char           # Prompt character
)
