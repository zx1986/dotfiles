services:
  ubuntu_verify:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dotfiles_ubuntu_verify
    # Keep running so we can exec into it
    command: tail -f /dev/null
    volumes:
      # Mount the xProfile repo to test apply
      - ../../:/home/testuser/xProfile:ro
